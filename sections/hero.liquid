<div class="hero">
    <div class="hero-image hero-image-large">
    </div>
    <div class="hero-image hero-image-small">
        <div class="hero-text">
            <div>{{ section.settings.heading }}</div>
            <a href="/collections/all" class="hero-button">
                    Explore our collection <span class="arrow">→</span>
            </a>
        </div>
    </div>
</div>

{% schema %}
{
    "name": "Hero Section",
    "settings": [
        {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Meet me at Midnight."
        },
        {
        "type": "url",
        "id": "button_link",
        "label": "Button Link",
        "default": "/collections/all"
        },
        {
        "type": "text",
        "id": "button_text",
        "label": "Button Text",
        "default": "Shop Now"
        }
    ]
}
{% endschema %}